import XCTest
@testable import SoulverTextKit

final class SoulverTextKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SoulverTextKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
